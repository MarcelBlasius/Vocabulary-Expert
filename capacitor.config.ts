import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Vocabulary Expert',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
